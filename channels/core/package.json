{
  "name": "channel-core",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Platform-independent MCP-over-HTTP channel daemon kernel, shared by telegram-channel and lark-channel. Platform I/O (inbound poll / outbound send / identity / config / cursor) is injected via the Platform interface.",
  "exports": {
    "./routing": "./src/routing.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "express": "^4.21.2"
  }
}
