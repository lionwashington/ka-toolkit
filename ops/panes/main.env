# Pane-specific env for team-lead. Sourced by start-pane.sh before `exec claude`.
# Keep secrets out of git — this file is gitignored via ops/.gitignore patterns
# if you put secrets here; the example is safe to commit.
